name: Build and Publish NuGet Packages

on:
  push:
    branches: [ develop, main ]
    paths:
      - '**/*.csproj'
      - '**/*.cs'
      - '.github/workflows/nuget-packages.yml'
  workflow_dispatch:
    inputs:
      publish_to_nuget:
        description: 'Publish to NuGet.org'
        required: false
        default: false
        type: boolean

env:
  DOTNET_VERSION: '9.0.x'
  BUILD_CONFIGURATION: 'Release'

permissions:
  contents: write       # Required to push commits back to repository
  actions: read         # Required to read workflow artifacts
  pull-requests: write  # Required to create pull requests

jobs:
  # Job 1: Build and test (with version management)
  build-and-test:
    runs-on: ubuntu-latest
    
    outputs:
      version: ${{ steps.version.outputs.version }}
      should_publish: ${{ steps.publish-check.outputs.should_publish }}
      
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Get and manage version
      id: version
      run: |
        # Get current version from project file using standard grep and sed
        CURRENT_VERSION=$(grep '<Version>' affolterNET.Web.Core/affolterNET.Web.Core.csproj | sed 's/.*<Version>\(.*\)<\/Version>.*/\1/' | tr -d ' ')
        echo "Found current version: '$CURRENT_VERSION'"
        
        # If version is empty, use a default starting version
        if [ -z "$CURRENT_VERSION" ]; then
          echo "No version found in project file, using default version 0.1.0"
          CURRENT_VERSION="0.1.0"
        fi
        
        echo "Current version: $CURRENT_VERSION"
        
        # On develop branch, bump the version (for pushes and merged PRs)
        if [ "${{ github.ref }}" == "refs/heads/develop" ] && [ "${{ github.event_name }}" == "push" ]; then
          # Parse version components and increment patch
          MAJOR=$(echo "$CURRENT_VERSION" | cut -d'.' -f1)
          MINOR=$(echo "$CURRENT_VERSION" | cut -d'.' -f2)
          PATCH=$(echo "$CURRENT_VERSION" | cut -d'.' -f3)
          
          NEW_PATCH=$((PATCH + 1))
          NEW_VERSION="$MAJOR.$MINOR.$NEW_PATCH"
          
          echo "Bumping version from $CURRENT_VERSION to $NEW_VERSION"
          
          # Update version in all project files
          find . -name "*.csproj" -exec sed -i "s/<Version>.*<\/Version>/<Version>$NEW_VERSION<\/Version>/g" {} \;
          
          # Display updated versions for verification
          echo "Updated project versions:"
          find . -name "*.csproj" -exec grep -H "<Version>" {} \;
          
          # Commit changes
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          git add **/*.csproj
          if git diff --staged --quiet; then
            echo "No version changes to commit"
          else
            git commit -m "chore: bump version to $NEW_VERSION"
            git push origin develop
            echo "Version bumped to $NEW_VERSION on develop branch"
          fi
          
          # Output the new version
          echo "version=$NEW_VERSION" >> $GITHUB_OUTPUT
        else
          # On other branches, just use the current version
          echo "Using current version: $CURRENT_VERSION"
          echo "version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
        fi

    - name: Check if should publish
      id: publish-check
      run: |
        if [ "${{ github.ref }}" == "refs/heads/main" ] && [ "${{ github.event_name }}" == "push" ]; then
          echo "should_publish=true" >> $GITHUB_OUTPUT
          echo "Will publish packages (main branch push)"
        else
          echo "should_publish=false" >> $GITHUB_OUTPUT
          echo "Will not publish packages (not main branch push)"
        fi

    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Restore dependencies
      run: dotnet restore

    - name: Build solution
      run: |
        echo "Building with version: ${{ steps.version.outputs.version }}"
        dotnet build affolterNET.Web.sln --configuration ${{ env.BUILD_CONFIGURATION }} --no-restore

    - name: Run tests
      run: |
        echo "Running tests..."
        dotnet test affolterNET.Web.sln --configuration ${{ env.BUILD_CONFIGURATION }} --no-build --verbosity normal --logger trx --results-directory ./test-results

    - name: Upload test results
      uses: actions/upload-artifact@v6
      if: always()
      with:
        name: test-results
        path: ./test-results/*.trx

  # Job 2: Pack and publish (only when publishing to main)
  pack-and-publish:
    needs: [build-and-test]
    runs-on: ubuntu-latest
    if: |
      needs.build-and-test.outputs.should_publish == 'true' || 
      (github.event_name == 'workflow_dispatch' && github.event.inputs.publish_to_nuget == 'true')
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Restore dependencies
      run: dotnet restore

    - name: Build projects first (with project references)
      run: |
        echo "Building projects with project references..."
        dotnet build affolterNET.Web.Core/affolterNET.Web.Core.csproj --configuration ${{ env.BUILD_CONFIGURATION }} --no-restore
        dotnet build affolterNET.Web.Api/affolterNET.Web.Api.csproj --configuration ${{ env.BUILD_CONFIGURATION }} --no-restore
        dotnet build affolterNET.Web.Bff/affolterNET.Web.Bff.csproj --configuration ${{ env.BUILD_CONFIGURATION }} --no-restore

    - name: Switch to NuGet references for packaging
      run: |
        ./switch-references.sh nuget
        echo "Switched to NuGet package references for packaging"

    - name: Pack NuGet packages
      run: |
        VERSION="${{ needs.build-and-test.outputs.version }}"
        echo "Packing packages with version $VERSION"
        
        # Pack Core package (must be first due to dependencies)
        dotnet pack affolterNET.Web.Core/affolterNET.Web.Core.csproj \
          --configuration ${{ env.BUILD_CONFIGURATION }} \
          --no-restore \
          --no-build \
          --output ./packages \
          --include-symbols \
          --include-source \
          -p:SymbolPackageFormat=snupkg
        
        # Pack Api package
        dotnet pack affolterNET.Web.Api/affolterNET.Web.Api.csproj \
          --configuration ${{ env.BUILD_CONFIGURATION }} \
          --no-restore \
          --no-build \
          --output ./packages \
          --include-symbols \
          --include-source \
          -p:SymbolPackageFormat=snupkg
        
        # Pack Bff package
        dotnet pack affolterNET.Web.Bff/affolterNET.Web.Bff.csproj \
          --configuration ${{ env.BUILD_CONFIGURATION }} \
          --no-restore \
          --no-build \
          --output ./packages \
          --include-symbols \
          --include-source \
          -p:SymbolPackageFormat=snupkg
        
        echo "Created packages:"
        ls -la ./packages/

    - name: Upload packages as artifacts
      uses: actions/upload-artifact@v6
      with:
        name: nuget-packages
        path: ./packages/*.nupkg

    - name: Upload symbol packages as artifacts
      uses: actions/upload-artifact@v6
      with:
        name: nuget-symbol-packages
        path: ./packages/*.snupkg

    - name: Publish to NuGet.org
      if: |
        needs.build-and-test.outputs.should_publish == 'true' ||
        (github.event_name == 'workflow_dispatch' && github.event.inputs.publish_to_nuget == 'true')
      run: |
        echo "Publishing packages to NuGet.org..."
        for package in ./packages/affolterNET.Web.Core.*.nupkg \
                      ./packages/affolterNET.Web.Api.*.nupkg \
                      ./packages/affolterNET.Web.Bff.*.nupkg; do
          # Skip if it's a symbol package
          if [[ $package == *.snupkg ]]; then
            continue
          fi
          
          echo "Publishing $package"
          dotnet nuget push "$package" \
            --api-key ${{ secrets.NUGET_API_KEY }} \
            --source https://api.nuget.org/v3/index.json \
            --skip-duplicate
        done
      env:
        NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}

    - name: Create and push git tag
      if: |
        needs.build-and-test.outputs.should_publish == 'true' ||
        (github.event_name == 'workflow_dispatch' && github.event.inputs.publish_to_nuget == 'true')
      run: |
        VERSION="${{ needs.build-and-test.outputs.version }}"
        TAG_NAME="v$VERSION"
        
        echo "Creating tag: $TAG_NAME"
        
        # Configure git
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        # Create annotated tag with release message
        git tag -a "$TAG_NAME" -m "Release $TAG_NAME

        📦 NuGet Packages Published:
        - affolterNET.Web.Core v$VERSION
        - affolterNET.Web.Api v$VERSION  
        - affolterNET.Web.Bff v$VERSION
        
        🔗 NuGet Gallery: https://www.nuget.org/profiles/affolterNET
        
        📋 Installation:
        dotnet add package affolterNET.Web.Core --version $VERSION
        dotnet add package affolterNET.Web.Api --version $VERSION
        dotnet add package affolterNET.Web.Bff --version $VERSION"
        
        # Push tag to origin
        git push origin "$TAG_NAME"
        
        echo "✅ Tag $TAG_NAME created and pushed to GitHub"
        echo "🏷️ View tag: https://github.com/${{ github.repository }}/releases/tag/$TAG_NAME"

    - name: Create release summary
      run: |
        VERSION="${{ needs.build-and-test.outputs.version }}"
        TAG_NAME="v$VERSION"
        echo "## 🎉 NuGet Packages Published" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Version:** $VERSION" >> $GITHUB_STEP_SUMMARY
        echo "**Tag:** [$TAG_NAME](https://github.com/${{ github.repository }}/releases/tag/$TAG_NAME)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📦 Published Packages:" >> $GITHUB_STEP_SUMMARY
        echo "- [affolterNET.Web.Core](https://www.nuget.org/packages/affolterNET.Web.Core/$VERSION)" >> $GITHUB_STEP_SUMMARY
        echo "- [affolterNET.Web.Api](https://www.nuget.org/packages/affolterNET.Web.Api/$VERSION)" >> $GITHUB_STEP_SUMMARY
        echo "- [affolterNET.Web.Bff](https://www.nuget.org/packages/affolterNET.Web.Bff/$VERSION)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 🚀 Installation:" >> $GITHUB_STEP_SUMMARY
        echo '```bash' >> $GITHUB_STEP_SUMMARY
        echo "dotnet add package affolterNET.Web.Core --version $VERSION" >> $GITHUB_STEP_SUMMARY
        echo "dotnet add package affolterNET.Web.Api --version $VERSION" >> $GITHUB_STEP_SUMMARY
        echo "dotnet add package affolterNET.Web.Bff --version $VERSION" >> $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 🔗 Links:" >> $GITHUB_STEP_SUMMARY
        echo "- [View Release](https://github.com/${{ github.repository }}/releases/tag/$TAG_NAME)" >> $GITHUB_STEP_SUMMARY
        echo "- [NuGet Profile](https://www.nuget.org/profiles/affolterNET)" >> $GITHUB_STEP_SUMMARY